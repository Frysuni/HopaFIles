{
  "private": true,
  "name": "hopafiles",
  "version": "0.0.0",
  "author": "Frys",
  "license": "UNLICENSED",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "boot": "npm i -g lerna yarn && lerna bootstrap",
    "build": "lerna run build",
    "clean": "lerna clean --yes && yarn cache clean"
  },
  "devDependencies": {
    "lerna": "^6.6.2"
  }
}
